import React from 'react';

const GifToolPage = () => {
    return (
        <div style={{ textAlign: 'center', display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', gap: '5vh', height: '100%' }}>
            <h1 style={{ fontSize: '4rem'}}>Gif Tool</h1>
            <p style={{ fontSize: '3rem'}}>Under Construction. Check back later.</p>
        </div>
    );
};

export default GifToolPage;